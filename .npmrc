# 本项目使用官方 npm 源（用户已确认，不强制用内网源）。
# 背景：package.json 的 devEngines 曾要求 pnpm@^11.22.0 且 onFail=download，
# 触发 pnpm"自动下载切换自身版本"，而内网源 (registry.npm.qima-inc.com)
# 上 pnpm 最新只有 11.20.0 且缺二进制包 @pnpm/exe，导致自锁。
# 已改为 onFail=ignore（直接用本机已装的 pnpm 11.22.0），此问题不再出现。
#
# 之后如果需要内网私有包，可再按 scope 追加：
#   @company:registry=https://registry.npm.qima-inc.com/
registry=https://registry.npmjs.org/
